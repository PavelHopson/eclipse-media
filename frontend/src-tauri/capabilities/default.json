{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability set for the local Eclipse Media window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
